import {SafeAreaView} from "react-native";
import styles from "./AddTransactionView.styles.ts";

const AddTransactionView = () => {
    return (
        <SafeAreaView style={styles.pageContainer}>

        </SafeAreaView>
    )
};
export default AddTransactionView;